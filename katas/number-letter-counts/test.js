const assert = require('assert');

describe('Number Letter Counts', function() {

  it('works', () => {
    const expected = true;
    const actual = false;

    assert.equal(actual, expected);
  });

});
