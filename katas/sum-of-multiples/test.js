const assert = require('assert');

describe('Multiple of 3 and 5', function() {

  it('works', () => {
    const expected = true;
    const actual = false;

    assert.equal(actual, expected);
  });

});
